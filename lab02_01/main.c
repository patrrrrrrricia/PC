#include <stdio.h>
#include <stdlib.h>

int main()
{
    //am demonstrat ca 100000 * 100000 trece de limita int-ului
    int a, b;
    scanf("%d%d", &a, &b);
    printf("a + b = %d\n", a+b);
    printf("a * b = %d\n", a*b);
    return 0;
}
