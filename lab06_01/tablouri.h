#ifndef TABLOURI_H_INCLUDED
#define TABLOURI_H_INCLUDED

void citire(int n, float a[]);
void afisare(int n, float a[]);
float Min(int n, float a[]);
int IndexMin(int n, float a[]);
int Sum(int n, float a[]);

#endif // TABLOURI_H_INCLUDED
